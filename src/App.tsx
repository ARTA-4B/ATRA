import { useEffect, useState } from 'react';
import { Dashboard } from './Dashboard';
import { PublicSite } from './Public';
export function App() { const [route,setRoute]=useState(window.location.hash || '#/'); useEffect(()=>{const onHash=()=>setRoute(window.location.hash || '#/');window.addEventListener('hashchange',onHash);return()=>window.removeEventListener('hashchange',onHash);},[]);useEffect(()=>{const anchor=route.indexOf('#',1);if(anchor>=0){requestAnimationFrame(()=>document.getElementById(route.slice(anchor+1))?.scrollIntoView({behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'}));}else window.scrollTo(0,0);const page=route.split('/')[2]||route.split('/')[1]||'Home';document.title=`ATRA — ${page.charAt(0).toUpperCase()+page.slice(1).replaceAll('-',' ')}`;},[route]);if(route.startsWith('#/app/')) return <Dashboard page={route.split('/')[2]||'overview'}/>;const page=route.slice(2).split('#')[0]||'home';return <PublicSite page={page}/>;}

import { useState } from 'react';
import { Activity, ArrowDown, ArrowDownToLine, ArrowRight, ArrowUpRight, Bot, ChartNoAxesCombined, Check, Code2, GitFork, HardDrive, Layers3, LockKeyhole, Menu, Monitor, Network, Radio, Server, ShieldCheck, SlidersHorizontal, SquareTerminal, Wallet, X } from 'lucide-react';
import { Badge, Button, ChainBadge, CopyButton, DataNotice, Logo, Modal, Panel, PortfolioChart, Segmented, TextLink, Warning } from './components';
import { chains } from './data/services';
import { MarketExplorer } from './Market';

const featureItems = [
  { icon: ChartNoAxesCombined, number: '01', title: 'Research before execution.', text: 'Turn market and on-chain data into structured research. Understand the context before an action is considered.', tag: 'MARKET INTELLIGENCE' },
  { icon: SlidersHorizontal, number: '02', title: 'Your strategy. Hard limits.', text: 'Automate supported trades within your position, liquidity, and slippage limits. Every action passes the risk engine.', tag: 'AUTO TRADE' },
  { icon: Layers3, number: '03', title: 'Liquidity, kept in check.', text: 'Monitor supported positions, track fees, and rebalance only when your configured policy allows it.', tag: 'AUTO LP' },
  { icon: Wallet, number: '04', title: 'An isolated Agent Wallet.', text: 'Keep agent capital separate from your primary wallet. You choose what to deposit and when to withdraw.', tag: 'USER-FUNDED' },
  { icon: Radio, number: '05', title: 'Stay close. From anywhere.', text: 'Check status, receive alerts, and pause your agent from Telegram. Your private keys never leave your machine.', tag: 'TELEGRAM CONTROL' },
  { icon: Server, number: '06', title: 'Your machine is the platform.', text: 'Run locally, on a home server, or on a VPS. Keep your runtime, configuration, and wallet under your control.', tag: 'SELF-HOSTED' },
];
export function DashboardPreview() { const [range, setRange] = useState('1W'); return <div className="terminal-preview"><div className="preview-chrome"><div className="window-dots"><i/><i/><i/></div><span><LockKeyhole size={10}/> local workspace / overview</span><Badge tone="lime">PAPER MODE</Badge></div><div className="preview-body"><aside><div className="mini-logo"><img src="/brand/heron.png" alt="" width="16" height="23" decoding="async"/></div>{[ChartNoAxesCombined,Activity,Bot,Layers3,Wallet,ShieldCheck].map((Icon,i)=><div key={i} className={i===0?'active':''}><Icon size={16}/></div>)}<div className="mini-bottom"><span className="status-dot"/></div></aside><div className="preview-main"><div className="preview-title"><div><span className="eyebrow">YOUR AUTONOMOUS WORKSPACE</span><h3>Overview<span className="muted"> /</span></h3></div><span className="preview-running"><span className="status-dot"/> Simulation</span></div><div className="preview-metrics"><div><span>Agent Wallet value</span><strong>$12,480<span>.62</span></strong><small>SIMULATED BALANCE</small></div><div><span>Today's P&L</span><strong className="positive">+$184.26</strong><small className="positive">↗ +1.50% simulated</small></div><div><span>Risk utilization</span><strong>24<span> / 100%</span></strong><div className="mini-progress"><i/></div></div></div><div className="preview-chart-head"><span>Portfolio performance</span><Segmented items={['1D','1W','1M']} value={range} onChange={setRange}/></div><PortfolioChart compact range={range}/><div className="preview-agent-row"><span><Bot size={15}/> Research Agent</span><span><span className="status-dot"/> Monitoring</span><span className="muted">Demo cycle</span></div><div className="preview-agent-row"><span><SlidersHorizontal size={15}/> Trader Agent</span><span><span className="status-dot neutral"/> Awaiting signal</span><span className="muted">Paper only</span></div><div className="preview-bottom"><ShieldCheck size={12}/><span>Deterministic risk checks enabled</span><span>DEMO DATA</span></div></div></div></div>; }

export function PublicSite({ page }: { page: string }) { const [menu, setMenu] = useState(false); const [repo, setRepo] = useState(false); const isHome = page === 'home'; return <div className="public-site"><header className="public-header"><div className="public-nav"><Logo/><nav aria-label="Main navigation" className={menu?'open':''}>{[['Home','#/'],['Market','#/market'],['Features','#/#features'],['Security','#/#security'],['Docs','#/docs']].map(([name,url])=><a key={name} className={(page==='home'&&name==='Home')||(page==='market'&&name==='Market')||(page==='docs'&&name==='Docs')?'active':''} href={url} onClick={()=>setMenu(false)}>{name}</a>)}</nav><div className="nav-actions"><button className="github-button" aria-label="Repository information" onClick={()=>setRepo(true)}><GitFork size={19}/></button><a className="button nav-launch" href="#/app/overview">Open dashboard<ArrowUpRight size={15}/></a><button className="icon-button mobile-menu" aria-label="Toggle navigation" aria-expanded={menu} onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button></div></div></header>
  <main>{isHome ? <><section className="hero public-container"><div className="hero-copy"><div className="hero-eyebrow"><span className="status-dot"/> OPEN SOURCE. ALWAYS YOURS.</div><h1>Autonomous by design.<br/><span>Yours by default.</span></h1><p className="hero-description">An autonomous crypto agent that runs on your machine. Research markets, manage trades, and monitor liquidity. <strong>Within the limits you define.</strong></p><div className="hero-buttons"><a className="button primary" href="#/install"><ArrowDownToLine size={16}/>Install ATRA<ArrowRight size={16}/></a><Button variant="ghost" onClick={()=>setRepo(true)}><GitFork size={17}/>View on GitHub<ArrowUpRight size={14}/></Button></div><div className="hero-assurances"><span><Check size={13}/>Self-hosted</span><span><Check size={13}/>User-funded</span><span><Check size={13}/>Paper mode by default</span></div></div><div className="hero-product"><div className="preview-caption"><span>01 / THE LOCAL WORKSPACE</span><span>BUILT FOR CONTROL<ArrowDown size={11}/></span></div><DashboardPreview/><div className="preview-footnote"><span>YOUR CAPITAL. YOUR RULES. YOUR MACHINE.</span><span>Interactive UI preview ↗</span></div></div></section>
  <section className="chain-strip public-container"><div><span className="eyebrow">FOUR CHAINS.</span><strong>One workspace.</strong></div>{chains.map(c=><ChainBadge key={c} chain={c}/>)}<a href="#/#networks" aria-label="Learn about supported chains"><ArrowUpRight size={19}/></a></section>
  <section id="features" className="features-section public-container"><div className="section-heading"><div><span className="eyebrow"><i/> PURPOSE-BUILT AUTONOMY</span><h2>More capability.<br/><span>Never less control.</span></h2></div><p>From the first observation to the final risk check,<br className="desktop-break"/> every step has a purpose. Every limit is yours.</p></div><div className="feature-grid">{featureItems.map(({icon:Icon,number,title,text,tag})=><article key={number}><div className="feature-top"><Icon size={23} strokeWidth={1.4}/><span>{number}</span></div><h3>{title}</h3><p>{text}</p><span className="feature-tag">{tag}<ArrowUpRight size={13}/></span></article>)}</div></section>
  <section className="market-section public-container"><div className="section-heading"><div><span className="eyebrow"><i/> A CLEARER VIEW OF THE MARKET</span><h2>Signal. With context.</h2></div><TextLink href="#/market">Explore markets</TextLink></div><Panel><div className="market-preview-head"><span><span className="status-dot neutral"/> Market overview</span><Badge>ILLUSTRATIVE DATA</Badge></div><MarketExplorer preview/></Panel><div className="section-footnote">A preview of the market explorer. Prices and charts shown are sample data, not live market information.</div></section>
  <section className="security-section" id="security"><div className="public-container security-grid"><div><span className="eyebrow"><i/> TRUST IS AN ARCHITECTURE</span><h2>Automation without<br/><span>surrendering control.</span></h2><p>Reasoning proposes. Your policies decide.<br/>The model never gets the keys.</p><a className="button ghost" href="#/docs/security">Explore the security model<ArrowUpRight size={15}/></a><div className="security-diagram"><div><Bot size={24}/><span>REASONING</span></div><span className="diagram-line"/><div className="highlight"><ShieldCheck size={24}/><span>RISK ENGINE</span></div><span className="diagram-line"/><div><Wallet size={24}/><span>EXECUTION</span></div></div></div><div className="security-list">{[['Local wallet vault','Private keys are designed to be encrypted and stored locally.'],['Deterministic risk engine','Hard limits are enforced outside the reasoning model.'],['Paper first. Live by choice.','Simulate behavior before deliberately enabling live execution.'],['An independent stop switch','Block new autonomous actions without relying on the model.']].map(([title,copy],i)=><div key={title}><span>0{i+1}</span><div><h3>{title}</h3><p>{copy}</p></div><ShieldCheck size={17}/></div>)}</div></div></section>
  <section id="how-it-works" className="how-section public-container"><div className="section-heading"><div><span className="eyebrow"><i/> FROM INSTALL TO AUTONOMY</span><h2>Your agent. In five steps.</h2></div><p>No subscription. No token requirement.<br/>Just your machine and a clear set of rules.</p></div><div className="steps-grid">{[['Install','Deploy locally or on a VPS.'],['Create your wallet','An isolated EVM wallet and a separate Solana keypair.'],['Fund on your terms','Deposit only the capital you want the agent to manage.'],['Set your limits','Define position sizes, slippage, and emergency controls.'],['Start in Paper Mode','Verify behavior before you enable live execution.']].map(([title,copy],i)=><article key={title}><div><span>0{i+1}</span>{i<4&&<ArrowRight size={15}/>}</div><h3>{title}</h3><p>{copy}</p></article>)}</div></section>
  <section id="networks" className="network-section public-container"><div><Network size={24}/><h3>Four networks. Two isolated wallets.</h3><p>Base, BNB Smart Chain, and Robinhood Chain share the EVM Agent Wallet. Solana uses its own keypair.</p></div><div className="network-groups"><div><span className="eyebrow">EVM WALLET</span><p>Base / BNB Smart Chain / Robinhood Chain</p></div><div><span className="eyebrow">SOLANA WALLET</span><p>Separate Solana keypair</p></div></div></section>
  <section className="install-teaser public-container"><div><span className="eyebrow"><i/> BUILT TO RUN ON YOUR TERMS</span><h2>Bring your own machine.<br/><span>We’ll bring the workspace.</span></h2><p>Desktop, home server, or VPS. Your machine must remain<br className="desktop-break"/> online for ATRA to operate continuously.</p><a className="button primary" href="#/install">Get started<ArrowRight size={16}/></a></div><div className="install-terminal"><div><SquareTerminal size={15}/><span>QUICK START</span><Badge>SETUP GUIDE</Badge></div><pre><span># Use the verified ATRA repository URL</span>{'\ngit clone <repository>\ncd ATRA\ndocker compose up -d'}</pre><footer><HardDrive size={14}/>Self-hosted runtime · See installation requirements</footer></div></section>
  <section className="token-section public-container"><span className="token-mark">$</span><div><h3>Open source. Not token-gated.</h3><p>$ATRA is an optional community support token. Holding $ATRA is not required to download, run, modify, or use ATRA.</p><small>Project infrastructure may be voluntarily funded by the creator, including with creator-fee revenue received from third-party launch platforms.</small></div><Badge>FREE TO USE</Badge></section></> : page==='market' ? <div className="public-container standalone"><div className="section-heading"><div><span className="eyebrow">MARKET INTELLIGENCE</span><h1>Markets, in perspective.</h1><p>Explore supported networks. Inspect the data before the decision.</p></div><Badge>DEMO ENVIRONMENT</Badge></div><Panel><MarketExplorer/></Panel></div> : page==='install' ? <InstallPage/> : <DocsPage page={page}/>}
  </main><footer className="public-footer"><div className="public-container"><div className="footer-main"><div><Logo/><p>Autonomous by design.<br/>Yours by default.</p></div><div><span>PRODUCT</span><a href="#/market">Markets</a><a href="#/#features">Features</a><a href="#/app/overview">Dashboard</a></div><div><span>RESOURCES</span><a href="#/install">Installation</a><a href="#/docs">Documentation</a><a href="#/docs/security">Security model</a></div><div><span>PROJECT</span><button onClick={()=>setRepo(true)}>GitHub <ArrowUpRight size={12}/></button><a href="#/docs/status">System status</a><a href="#/#how-it-works">How it works</a></div></div><div className="footer-bottom"><span>© {new Date().getFullYear()} ATRA. Open source. User controlled.</span><span><span className="status-dot neutral"/>Frontend preview<span className="footer-divider">/</span>No runtime connected</span><span>BUILT FOR INDEPENDENCE ↗</span></div></div></footer>{repo&&<Modal title="Source repository" onClose={()=>setRepo(false)}><GitFork size={28}/><p>The official ATRA repository URL has not been configured for this preview. No unverified repository is linked.</p><p className="muted">The complete frontend source is included in this project. See the installation guide to run it locally.</p><a className="button primary" href="#/install" onClick={()=>setRepo(false)}>View installation guide<ArrowRight size={16}/></a></Modal>}</div>; }

function InstallPage() { const [platform,setPlatform]=useState('Desktop'); const command='npm install\nnpm run dev'; return <div className="public-container standalone install-page"><span className="eyebrow">GET STARTED / SELF-HOSTED</span><h1>A workspace of your own.</h1><p className="lead">Start with the frontend. Connect your local runtime when it is available.</p><Segmented items={['Desktop','Home server','VPS']} value={platform} onChange={setPlatform}/><div className="two-columns"><Panel title={`Run on your ${platform.toLowerCase()}`}><div className="panel-content"><div className="numbered-copy"><span>01</span><div><h3>Prepare your environment</h3><p>Install Node.js 22.12+ and npm. Open a terminal in the frontend project folder.</p></div></div><div className="code-block"><header><span>FRONTEND / DEVELOPMENT</span><CopyButton value={command}/></header><pre>{command}</pre></div><div className="numbered-copy"><span>02</span><div><h3>Open your workspace</h3><p>Visit the local URL printed by Vite, usually http://localhost:5173. Open the dashboard to explore the demo or begin setup.</p></div></div><div className="numbered-copy"><span>03</span><div><h3>Build for deployment</h3><p>Run <code>npm run build</code>. Serve the generated <code>dist</code> directory with a static web server.</p></div></div>{platform!=='Desktop'&&<Warning>{platform==='VPS'?'Keep your dashboard behind authentication and a private network. Do not expose a wallet runtime directly to the public internet.':'Keep the host powered on and restrict dashboard access to your trusted local network.'}</Warning>}<a className="button primary" href="#/app/setup">Open setup<ArrowRight size={15}/></a></div></Panel><div><Panel title="Runtime installation"><div className="panel-content"><Warning>The agent backend, wallet vault, and Docker deployment are not included in this frontend project.</Warning><p>The runtime installation below is a template. Replace the repository placeholder with the verified project URL and follow its runtime documentation.</p><pre className="plain-code">{'git clone <repository>\ncd ATRA\ndocker compose up -d'}</pre><p>Your machine must remain online for ATRA to operate continuously.</p></div></Panel><div className="deploy-options">{[Monitor,HardDrive,Server].map((Icon,i)=><div key={i}><Icon size={20}/><span>{['Desktop','Home server','VPS'][i]}</span></div>)}</div></div></div></div>; }
function DocsPage({page}:{page:string}) { const topic=page.split('/')[1]; return <div className="public-container standalone docs-page"><span className="eyebrow">ATRA / DOCUMENTATION</span><h1>{topic==='security'?'Control is a design requirement.':topic==='status'?'A clear view of system status.':'Understand your workspace.'}</h1><p className="lead">Practical guides for setup, operation, and the boundaries of this frontend preview.</p>{topic==='status'?<Panel title="Integration status" action={<Badge>FRONTEND PREVIEW</Badge>}><div className="status-list">{[['Public website','Available'],['Dashboard & paper UI','Available'],['Local agent runtime','Not connected'],['Market data provider','DexScreener (public API, live)'],['Wallet vault & transaction signing','Not connected'],['Telegram bot','Not configured'],['ATRA-4B','Trained weights not installed']].map(([a,b])=><div key={a}><span>{a}</span><Badge tone={b==='Available'?'lime':''}>{b}</Badge></div>)}</div></Panel>:topic==='security'?<><Warning>These describe the intended runtime security boundaries. This frontend does not implement a wallet vault, signing, or a deterministic execution engine.</Warning><div className="docs-grid">{[['Local custody','Private keys must be encrypted in the local runtime. The frontend contains no keys and does not accept private keys.'],['Reasoning without secrets','The model receives observations, never wallet private keys. Telegram is a control channel, not a key export channel.'],['Policy before execution','Trade size, loss, slippage, and allowlists must be enforced by the runtime before signing. Frontend validation is not a security boundary.'],['Deliberate live activation','Live execution requires runtime-verified funding, gas, reviewed limits, and local re-authentication. It is blocked in this preview.'],['Emergency stop','Stop blocks new autonomous actions. Already-broadcast transactions cannot be reversed. Wallet access remains available.'],['Protected local export','A connected runtime must re-authenticate the user before any local export. This preview never generates or reveals keys.']].map(([a,b])=><Panel key={a} title={a}><p className="panel-content">{b}</p></Panel>)}</div></>:<><div className="docs-grid">{[{icon:SquareTerminal,title:'Installation',copy:'Run the frontend locally and prepare for runtime integration.',href:'#/install'},{icon:ShieldCheck,title:'Security model',copy:'Understand custody, risk enforcement, and execution boundaries.',href:'#/docs/security'},{icon:Activity,title:'Integration status',copy:'See what is available and what requires a connected runtime.',href:'#/docs/status'},{icon:Wallet,title:'Agent Wallet',copy:'Explore deposits, withdrawal review, and protected export flows.',href:'#/app/wallet'},{icon:SlidersHorizontal,title:'Risk controls',copy:'Configure example capital, execution, and market quality limits.',href:'#/app/risk'},{icon:Code2,title:'First-run setup',copy:'Choose networks, set your baseline, and start in Paper Mode.',href:'#/app/setup'}].map(({icon:Icon,title,copy,href})=><a className="doc-card" href={href} key={title}><Icon size={23}/><h3>{title}</h3><p>{copy}</p><ArrowUpRight size={18}/></a>)}</div><Panel title="Paper Mode, explained"><div className="panel-content"><p>Paper Mode simulates execution without broadcasting transactions. In this frontend preview, market prices are live from DexScreener; balances, charts, and activity are static fixtures. They do not represent real account performance.</p><p>You fund the Agent Wallet. ATRA never provides trading capital. $ATRA is optional and is not required to use the software.</p><DataNotice/></div></Panel></>}</div>; }


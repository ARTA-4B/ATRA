# GPU training dependencies.
#
# torch is pinned below 2.13 because unsloth declares that bound; installing it
# unpinned from the cu130 index pulls 2.14 and pip then silently downgrades to a
# CPU-only build, which trains at a few percent of the expected speed while
# appearing to work.
#
# Install torch first, from the CUDA index:
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements-train.txt

unsloth==2026.9.7
unsloth_zoo==2026.9.6
transformers==4.62.1
trl==0.27.0
peft==0.19.1
accelerate==1.12.0
bitsandbytes==0.50.2
datasets==4.5.0
sentencepiece==0.2.1
huggingface_hub==0.38.2
pyyaml==6.0.3
